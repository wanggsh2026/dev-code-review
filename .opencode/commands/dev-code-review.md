# Dev Code Review

Run the GitLab merge request code review workflow:

```bash
bash gitlab-merge-review/scripts/run-gitlab-merge-review.sh
```

Then read `review-output/review-report.json` and summarize the result.
